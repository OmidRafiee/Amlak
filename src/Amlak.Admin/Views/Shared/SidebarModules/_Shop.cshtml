
@if (true)
{

    <div class="navbar-title">
        <span>فروشگاه</span>
    </div>

    <ul class="main-navigation-menu">

        @if (User.IsInRole("Admin") || User.IsInRole("Product"))
        {
            <li data-menu-role="Calabin/Product">
                <a asp-controller="Product" asp-action="Index">
                    <div class="item-content">
                        <div class="item-media">
                            <i class="ti-package"></i>
                        </div>
                        <div class="item-inner">
                            <span class="title"> مدیریت محصول</span>
                        </div>
                    </div>
                </a>
            </li>
        }

        @if (User.IsInRole("Admin") || User.IsInRole("Product"))
        {
            <li data-menu-role="Clabin/ProductCreate">
                <a asp-controller="Product" asp-action="Create">
                    <div class="item-content">
                        <div class="item-media">
                            <i class="ti-ruler-pencil"></i>
                        </div>
                        <div class="item-inner">
                            <span class="title"> ایجاد محصول جدید</span>
                        </div>
                    </div>
                </a>
            </li>
        }


        @if (User.IsInRole("Admin") || User.IsInRole("CoinsPackage"))
        {
            <li data-menu-role="Calabin/CoinsPackage">
                <a asp-controller="CoinsPackage" asp-action="Index">
                    <div class="item-content">
                        <div class="item-media">
                            <i class="ti-package"></i>
                        </div>
                        <div class="item-inner">
                            <span class="title"> مدیریت بسته ها</span>
                        </div>
                    </div>
                </a>
            </li>
        }
        @if (User.IsInRole("Admin") || User.IsInRole("CoinsPackage"))
        {
            <li data-menu-role="Calabin/CoinsCreate">
                <a asp-controller="CoinsPackage" asp-action="Create">
                    <div class="item-content">
                        <div class="item-media">
                            <i class="ti-ruler-pencil"></i>
                        </div>
                        <div class="item-inner">
                            <span class="title"> ایجاد بسته جدید</span>
                        </div>
                    </div>
                </a>
            </li>
        }


        @if (User.IsInRole("Admin") || User.IsInRole("Category"))
        {
            <li data-menu-role="Calabin/Category">
                <a asp-controller="Category" asp-action="Index">
                    <div class="item-content">
                        <div class="item-media">
                            <i class="ti-package"></i>
                        </div>
                        <div class="item-inner">
                            <span class="title"> مدیریت گروه محصولات</span>
                        </div>
                    </div>
                </a>
            </li>
        }
        @if (User.IsInRole("Admin") || User.IsInRole("Category"))
        {
            <li data-menu-role="Calabin/CategoryCreate">
                <a asp-controller="Category" asp-action="Create">
                    <div class="item-content">
                        <div class="item-media">
                            <i class="ti-ruler-pencil"></i>
                        </div>
                        <div class="item-inner">
                            <span class="title"> ایجاد گروه جدید</span>
                        </div>
                    </div>
                </a>
            </li>
        }


    </ul>
}
